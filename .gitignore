# Dependencies
node_modules
.pnp
.pnp.js

# Testing
coverage
test-results
playwright-report
.playwright

# Production
dist
build

# Database dumps written by server/backup.js - the family's full financial
# history in plain text. Never commit these.
backups
*.backup.json

# Local server secrets and user exports. Keep server/.env.example tracked.
server/.env.backup
server/backup_before_multiaccounts/
server/**/*.xlsx

# Local bank API keys, configuration and authorization callback files.
server/.banking/

# Misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Бинарник mongod, который mongodb-memory-server качает для тестов роутов.
# Лежит здесь, а не в node_modules/.cache: оттуда его сносит каждая
# переустановка зависимостей, и ~780 МБ качаются заново.
.cache

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
