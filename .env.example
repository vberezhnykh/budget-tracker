# Frontend configuration. Copy to .env.local for local development.
# Logo.dev public publishable key (pk_...) only; never use a secret sk_ key.
# Vite includes this value in the client bundle at build time.
# Leave empty to use category icons without external logo requests.
VITE_LOGO_DEV_PUBLISHABLE_KEY=
