# Бэклог

Список того, чего в проекте нет. Составлен по итогам аудита кода (фронт,
сервер, тесты, конфиги) в августе 2026. Порядок внутри разделов - по
убыванию отдачи, а не по сложности.

Отметки: `[ ]` - не начато, `[~]` - в работе, `[x]` - сделано.

## Инженерная обвязка

### [x] CI

`.github/workflows/` не существовало: `npm run lint`, `npm test` и
`npm run test:e2e` не запускались автоматически ни на пуше, ни на пул-реквесте.
Тесты, которые никто не гоняет, деградируют за месяц.

Попутно вскрылось, что `npm run lint` был красный - 184 ошибки `no-undef` на
`require`/`process`/`module`: `eslint.config.js` описывал только браузерный
ES-модульный рантайм, а `server/**` - это CommonJS под Node.

Сделано: `.github/workflows/ci.yml` - две работы на пуш в любую ветку и на
пул-реквест. `checks`: линт, `npm test` (фронтенд в jsdom + юнит-тесты
сервера одним прогоном) и продакшен-сборка. `e2e`: смоук-сьют Playwright в
Chromium с кешем браузера и выгрузкой скриншотов при падении. Базы данных не
требует ни та, ни другая. Заодно починен линт: `server/**` описан как
CommonJS под Node, тесты сервера - как ES-модули, две пустые catch-привязки
убраны.

### [x] Валидация в `PUT /api/transactions/:id`

В отличие от POST, апдейт не проверял ни обязательные поля, ни типы, а
`findByIdAndUpdate` по умолчанию не применяет схему (`runValidators` не был
передан). То есть через PUT можно было записать `type: 'ерунда'` в обход
`enum` из `server/models/Transaction.js:15`, и такая операция ломала подсчёты
на фронте: `transformTransactions` выводит знак суммы из типа, и тип вне
перечисления молча попадал в «минус». Тем же путём проходили нулевая и
отрицательная сумма и нечитаемая дата.

Сделано: `server/transactionInput.js` - проверка и приведение тела запроса
отдельным модулем (как чистые куски аутентификации в `auth.js`, чтобы
покрывалось юнит-тестами без сервера и базы), 37 тестов в
`server/transactionInput.test.js`. Роут вызывает его до записи и передаёт
`runValidators: true`. Обновление осталось частичным: поле, которого нет в
теле запроса, не попадает в объект обновления и сохранённое значение не
трогает.

### [ ] `toAccount` остаётся от бывшего перевода

Найдено при разборе предыдущего пункта. Форма редактирования удаляет
`toAccount` из тела запроса, когда операция не перевод (см. `handleSubmit` в
`src/components/AddTransactionForm.jsx`), а частичное обновление отсутствующие
поля не трогает. Поэтому если перевод переделать в расход, `toAccount` от него
останется в документе. Сейчас безвредно - у не-перевода это поле нигде не
читается, - но это ложные данные в базе, и любой будущий отчёт по переводам
на них споткнётся. Чинится там же: при явной смене типа на не-перевод поле
нужно снимать через `$unset`.

### [ ] Индексы и пагинация транзакций

В `server/models/Transaction.js` нет ни одного индекса, а
`GET /api/transactions` (`server/index.js:487`) отдаёт всю историю разом, без
пагинации; фронт грузит её целиком на старте. Сейчас операций немного, но
объём растёт линейно и навсегда: через два-три года это секунды на холодном
старте с телефона. Минимум - индекс по `date`, дальше - отдача по периоду.

### [ ] `helmet` на сервере

Нет ни CSP, ни `X-Content-Type-Options`, ни `X-Frame-Options`. Одна строчка
`app.use(helmet())` плюс настройка CSP под инлайновые стили, которых в
компонентах много.

### [ ] Разблокировать зум

`index.html:8`: `maximum-scale=1.0, user-scalable=no`. Прямое нарушение
WCAG 1.4.4 - человек с плохим зрением не может увеличить экран. Обычно это
ставят от «страница дёргается при фокусе на инпут», но в iOS 10+ проблема
лечится размером шрифта в полях (≥16px), а не запретом зума.

### [ ] `<html lang="ru">`

`index.html:2` объявляет `lang="en"` при полностью русском интерфейсе.
Скринридер читает русский текст английским голосом. Правка на одну строку.

### [ ] Тёмная тема

В `src/index.css` нет ни одного `prefers-color-scheme`. Вся палитра уже живёт
в CSS-переменных, так что это один медиа-блок с переопределением токенов -
а приложением пользуются вечером с телефона.

### [ ] PWA: манифест, иконки, service worker

Ни манифеста, ни иконок (в `public/` только дефолтный `vite.svg`), ни service
worker. Мета-теги `mobile-web-app-capable` в `index.html` без манифеста ничего
не делают. Для ежедневного мобильного приложения «добавить на домашний экран»
и работа при плохой связи - самое заметное улучшение.

### [ ] Тесты серверных роутов

`server/auth.test.js` и `server/backup.test.js` есть, а ~685 строк CRUD-логики
не покрыты ничем - включая переименование категории, которое каскадом
переписывает все транзакции (`server/index.js:375`). Supertest +
mongodb-memory-server.

### [ ] Разгрузить `App.jsx`

1170 строк: весь слой загрузки данных, все обработчики и вся раскладка в одном
компоненте. Не горит, но каждый следующий фича-запрос дороже предыдущего.
Напрашивается вынос загрузки/мутаций в хуки (`useTransactions`, `useAccounts`).

### [ ] Оптимистичные обновления и ретраи

Каждое действие ждёт round-trip до Render; на мобильном интернете это ощутимая
пауза, а при обрыве связи - потеря введённого.

### [ ] Сбор ошибок с продакшена

`ErrorBoundary` показывает экран пользователю, но никуда не сообщает. Падение
на телефоне жены останется незамеченным.

## Функциональность

### [ ] Импорт банковской выписки

Похоже, фича №1. В `server/` лежат одноразовые скрипты ручного импорта
(`bulk_import.js`, `import_december*.js`), то есть историю уже вбивали руками,
и `xlsx` уже в зависимостях сервера. Загрузка CSV из Revolut с предпросмотром
и автоподбором категории по названию убирает основную рутину.

### [ ] Регулярные операции

Аренда, подписки, зарплата. Для семейного бюджета это половина всех записей, и
сейчас каждая вбивается заново каждый месяц.

### [ ] Лимиты по категориям

Сейчас лимит ровно один, общий (`monthlyLimit`, по умолчанию 7000,
`server/index.js`). «Продукты - 900, Кафе и доставка - 400» полезнее одной
общей цифры, а кольцо в карточке сводки уже готово показывать это по
категориям.

### [ ] Мультивалютность

`€` захардкожен в 37 местах по компонентам. Пока всё в евро - не проблема, но
любая поездка или покупка в долларах ломает картину.

### [ ] Цели накоплений

Механика «заморожено» / `excludeFromTotal` уже есть; отсюда недалеко до
полноценной цели: «на отпуск - 2000 из 3000, осталось 4 месяца».

### [ ] Фото чека к операции

### [ ] Undo для удаления

Обе мелкие, обе про доверие к приложению.
